# =============================================================================
# PAYLOAD CMS ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to Git!
#
# Generate secrets with: openssl rand -base64 32
# =============================================================================

# -----------------------------------------------------------------------------
# PAYLOAD CONFIGURATION
# -----------------------------------------------------------------------------

# JWT Secret - REQUIRED
# Generate with: openssl rand -base64 32
# This encrypts authentication tokens
PAYLOAD_SECRET=

# Server URL - REQUIRED
# Development: http://localhost:3000
# Production: https://your-site.vercel.app
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# DATABASE - Vercel Postgres
# -----------------------------------------------------------------------------
# Get these from: Vercel Dashboard → Storage → Postgres

POSTGRES_URL=
POSTGRES_DATABASE_URL=
POSTGRES_HOST=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Pooled connection (recommended)
POSTGRES_PRISMA_URL=

# Non-pooled (for migrations)
POSTGRES_URL_NON_POOLING=
POSTGRES_URL_NO_SSL=

# Vercel-specific
POSTGRES_NEON_PROJECT_ID=
NEXT_PUBLIC_POSTGRES_STACK_PROJECT_ID=
NEXT_PUBLIC_POSTGRES_STACK_PUBLISHABLE_CLIENT_KEY=
POSTGRES_STACK_SECRET_SERVER_KEY=

# -----------------------------------------------------------------------------
# FILE STORAGE - Vercel Blob
# -----------------------------------------------------------------------------
# Get from: Vercel Dashboard → Storage → Blob

BLOB_READ_WRITE_TOKEN=

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# Email adapter (if using email functionality)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Analytics (if using)
# NEXT_PUBLIC_GA_ID=
# NEXT_PUBLIC_GTAG_ID=

# =============================================================================
# NOTES:
# =============================================================================
#
# 1. Local Development (.env.local):
#    - Use localhost URLs
#    - Can use local or production database
#
# 2. Production (Vercel Environment Variables):
#    - Set in Vercel Dashboard → Settings → Environment Variables
#    - Use production URLs (https://your-site.vercel.app)
#    - Same database for all environments (or separate if preferred)
#
# 3. Security:
#    - Never commit .env files
#    - Use different PAYLOAD_SECRET for prod vs dev
#    - Rotate secrets if compromised
#
# 4. Vercel CLI Commands:
#    - Pull production env: vercel env pull .env.production
#    - Link project: vercel link
#
# =============================================================================
